#include <iostream>
#include <vector>

using namespace std;

int main() {
    vector<int> nums = {1, 2, 3, 4, 5};
    int count = 0;
    for(auto x : nums) {
        if(x > 2) {
            count++;
        }
    }
    cout << count << endl;
}
